DISCLOSURE INTELLIGENCE - DEEP PARSER VERSION

This version reads the underlying SEC filing and stores structured facts for the dashboard.
Upload the CONTENTS of this folder to the ROOT of the existing GitHub repository and replace matching files.
Keep the existing Render SEC_USER_AGENT environment variable.

After commit:
1. Wait for Render to deploy the new commit.
2. Confirm it is Live.
3. Hard-refresh the site with Ctrl+Shift+R.
4. Click Refresh market now once to re-ingest current filings with the new deep parsers.

IMPORTANT: Older database rows were parsed by earlier code and may not contain deep details. New/reprocessed rows will.
